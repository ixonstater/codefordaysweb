import 'package:flutter/material.dart';

class WebsiteTheme{
    static const primaryColor = const Color.fromRGBO(13, 20, 57, 1.0);
    static const backgroundColor = const Color.fromRGBO(226, 242, 245, 1.0);
    static const buttonColor = const Color.fromRGBO(60, 89, 241, 1.0);
    static const accentColor = const Color.fromRGBO(33, 209, 238, 1.0);

    static const primaryTextColor = Color(0xFFFFFFFF);
}