import 'package:flutter/material.dart';
import '../resources/text.dart';

class ProjectsPage extends StatelessWidget{
  ProjectsPage({Key key}) : super(key: key);

  @override
  Widget build (BuildContext build){
    return Container(
      child: Text(
        "Hello hosted projects page."
      )
    );
  }
}